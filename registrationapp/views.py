from django.shortcuts import render
from django.http import HttpResponse
def register(request):
    return render(request,'register.html')
def registration(request):
    request.GET["name"]
    request.GET["name"]
    request.GET["name"]
    request.GET["name"]
    request.GET["name"]
    request.GET["name"]
    request.GET["pin code"]
    request.GET["REGISTER"]
    return HttpResponse("<html><body bgcolor=green><h1>Registered Successfully</h1></body></html>")
# Create your views here.
